{
  "compilerOptions": {
    "outDir": "./dist/",  //указывает путь, куда будут скомпилированы все файлы TypeScript
    "noImplicitAny": true, //запрещает неявное присваивание типа "any" переменным и параметрам функций, требуя явного указания типа, но явно указать any все таки получиться
    "module": "ESNext", // указывает, что модули будут компилироваться в формат ES6
    "target": "es5", //  указывает, что код будет компилироваться в ES5, чтобы быть совместимым с более старыми версиями JavaScript
    "jsx": "react-jsx", // указывает, что будет использоваться синтаксис JSX для работы с React
    "allowJs": true, // компилятор будет обрабатывать не только TS файлы но и JS файлы
    "moduleResolution": "node",
    "esModuleInterop": true, // позволяет работать с пакетами, которые используют common js как с обычными пакетами с помощью import
    "allowSyntheticDefaultImports": true, // позволяет импортировать модули с использованием синтаксиса импорта по умолчанию, даже если модуль не экспортирует значение по умолчанию.
    "baseUrl": "." // для абсолютного импорта
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}